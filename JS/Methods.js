const calculator = {
    ok: function(a,b) {  //key- value pair , ok is key or say function name and in value section function we write to execute under the object known as Methods.
        return a+b;
    },
    sub : function(a,b) {
        return a - b;
    }
};